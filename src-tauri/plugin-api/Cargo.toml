[package]
name = "plugin-api"
version = "0.1.0"
edition = "2021"
description = "Shared plugin API types for memoq-clone WASM plugins"

[lib]
crate-type = ["rlib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
