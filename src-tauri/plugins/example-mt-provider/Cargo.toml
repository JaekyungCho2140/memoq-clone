[package]
name = "example-mt-provider"
version = "0.1.0"
edition = "2021"
description = "Example custom MT provider plugin for memoq-clone"

[lib]
crate-type = ["cdylib"]

[dependencies]
plugin-api = { path = "../../plugin-api" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
